using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Shop
{
    public Fish[] list;

    public void init()
    {
        Fish fish = new Fish(0, "test", "testing purpose", 1);
        //list = { fish};
    }
   
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
